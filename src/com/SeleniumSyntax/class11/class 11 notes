Selenium Class 11

If regular click does not work, you can use the alternative
way of having Actions class click method

Upload Documents:
In order to upload using selenium, we simply do sendKeys(filePath)

If Sendkeys does not work, you can use Robot class

JavaScript Executor:
it is a library in selenium which can be executed in different programming languages
(Java, C#, python, etc)

JSExecutor is an interface

Actions which can be performed by using JavaScript Executor:
Click on the element-->last option if regular click and actions click dont work
                            js.executeScript("arguments[0].style.backgroundColor='green'",password);


scroll through the page
refresh the page
highlight an element
scroll to a specific element
open a new tab
